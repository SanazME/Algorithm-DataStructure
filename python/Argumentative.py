#!/usr/bin/env python3
import sys
print (' '.join(sys.argv[1:]))
# sys.arg[0] is the name of the script
# sys.arg returns a list of input strings
# st.join(list..) : returns elements of list
# in string format separated by st string
